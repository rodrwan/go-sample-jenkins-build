apiVersion: v1
kind: Pod
metadata:
  name: webapp
  labels:
    app: webapp
spec:
  containers:
    - name: webapp
      image: repositoryUrl/webapp:tagVersion
      ports:
        - containerPort: 8080